# Vector Database Environment Variables
# Copy this file to .env and fill in your actual values

# Cohere API Key (required for embedding generation)
# Get your API key from: https://dashboard.cohere.com/api-keys
COHERE_API_KEY=your_cohere_api_key_here

# Optional: Embedding model to use (default: embed-english-v3.0)
EMBEDDING_MODEL=embed-english-v3.0

# Optional: Embedding dimension (default: 1024)
# Only use if you want to reduce dimensionality
# EMBEDDING_DIMENSION=1024

# Optional: Data directory (default: ./data)
# VECTOR_DB_DATA_DIR=./data
