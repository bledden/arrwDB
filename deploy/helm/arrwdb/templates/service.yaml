apiVersion: v1
kind: Service
metadata:
  name: {{ include "arrwdb.fullname" . }}
  labels:
    {{- include "arrwdb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arrwdb.selectorLabels" . | nindent 4 }}
